/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',  // tells Next.js to generate a static site
  // Optional: if hosted under a GitHub Pages subpath:
  // basePath: '/your-repo-name',
};

module.exports = nextConfig;
